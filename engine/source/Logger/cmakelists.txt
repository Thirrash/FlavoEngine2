project(Logger)
file(GLOB_RECURSE SOURCES_Logger CONFIGURE_DEPENDS
    "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/*.h"
)
add_library(Logger SHARED "${SOURCES_Logger}")
target_include_directories(Logger PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}")
target_include_directories(Logger PUBLIC "${FLAVO_ENGINE_SRC_DIR}")
target_compile_definitions(Logger PRIVATE LOGGER_API_EXPORT)
target_link_libraries(Logger PUBLIC Core)

set_target_properties(Logger PROPERTIES FOLDER Engine)
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${SOURCES_Logger})
