project(GameCore)
file(GLOB_RECURSE SOURCES_GameCore CONFIGURE_DEPENDS
    "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp"
    "${CMAKE_CURRENT_SOURCE_DIR}/*.h"
)
add_library(GameCore SHARED "${SOURCES_GameCore}")
target_include_directories(GameCore PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}")
target_include_directories(GameCore PRIVATE "${FLAVO_ENGINE_SRC_DIR}")
target_compile_definitions(GameCore PRIVATE GAME_CORE_API_EXPORT)
target_link_libraries(GameCore PUBLIC Core)
target_link_libraries(GameCore PUBLIC Logger)

set_target_properties(GameCore PROPERTIES FOLDER Game)
source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${SOURCES_GameCore})
